{% extends 'admin_base.html.twig' %}

{% block title_admin %}
  Newsletter
{% endblock %}

{% set date_format = 'd/m/Y H:i' %}

{% block admin_content %}
  <h1>Newsletter</h1>

  <div class="mb-2 ms-1">Object :</div>
  <div class="form-control mb-4">{{ newsletter.object }}</div>

  <div class="mb-2 ms-1">Body :</div>
  <div class="align-items-stretch mb-4" data-controller="markdown">
    <div class="d-none">
      <textarea class="form-widget p-3" data-markdown-target="input" data-action="input->markdown#render">{{ newsletter.body }}</textarea>
    </div>
    <div class="overflow-y-scroll markdown-form-render-container form-control p-3" style="max-height: 25rem; min-height: 14.09rem;" data-markdown-target="preview"></div>
  </div>

  <div class="mb-2 ms-1">CTA :</div>
  <div class="form-control mb-4">{{ newsletter.cta }}</div>

  <div class="mb-2 ms-1">Date de création :</div>
  <div class="form-control mb-4">{{ newsletter.createdAt|date(date_format) }}</div>

  <div class="mb-2 ms-1">Dernière mise à jour :</div>
  <div class="form-control mb-4">{{ newsletter.updatedAt ? newsletter.updatedAt|date(date_format) : 'Aucune mise à jour' }}</div>

  <div class="mb-2 ms-1">Date d'envoie :</div>
  <div class="form-control mb-4">{{ newsletter.sendAt ? newsletter.sendAt|date(date_format) : 'Non envoyé' }}</div>
{% endblock %}
