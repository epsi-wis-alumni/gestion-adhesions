{% import 'macros/fontawesome.html.twig' as fa %}
{% import "macros/modal.html.twig" as modal %}

{% set modalDeleteId = 'confirmAlert-delete-' ~ newsletter.id %}
<form method="post" action="{{ path('app_admin_newsletter_delete', { id: newsletter.id }) }}">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newsletter.id) }}" />
  <a class="bg-transparent border-0 link-danger" data-bs-toggle="modal" data-bs-target="#{{ modalDeleteId }}" href="#">
    {{ fa.icon('trash-can') }}
  </a>
  {{ modal.confirm(
    id: modalDeleteId,
    title: 'Êtes-vous sur de supprimer ?',
    continueButton: 'Supprimer',
    type: 'submit',
    accent: 'danger',
  ) }}
</form>