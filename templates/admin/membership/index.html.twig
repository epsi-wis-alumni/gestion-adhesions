{% extends 'admin_base.html.twig' %}

{% block title %}Gestions Adhésion{% endblock %}

{% block admin_content %}
  <div class="my-5">
    <div class="table-responsive border rounded">
      <table class="table table-striped mb-0">
        <thead class="table-bordered">
          <tr>
            <th>id</th>
            <th>email</th>
            <th>lastname</th>
            <th>firstname</th>
            <th>company</th>
            <th>job_title</th>
            <th>action</th>
          </tr>
        </thead>
        <tbody>
          {% for user in users %}
            <tr>
              <td>{{ user.id }}</td>
              <td>{{ user.email }}</td>
              <td>{{ user.lastname }}</td>
              <td>{{ user.firstname }}</td>
              <td>{{ user.company }}</td>
              <td>{{ user.jobTitle }}</td>
              <td class="d-flex justify-content-center align-items-center gap-1">
                <a class="btn btn-primary" href="{{ path('app_admin_user_update', { 'id': user.id }) }}">Modifier</a>
                {% include "admin/user/_delete.html.twig" %}
              </td>
            </tr>
          {% else %}
            <tr>
              <td colspan="7" class="text-center">Aucun utilisateur</td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
{% endblock %}
