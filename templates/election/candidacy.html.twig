{% extends 'base.html.twig' %}
{% import 'macros/fontawesome.html.twig' as fa %}

{% block title %}
  Candidature
{% endblock %}

{% block body %}
  <div class="container-form my-5 w-100">
    {{ form_start(form) }}
    {{ form_row(form.presentation) }}
    <div class="text-end">
      <button type="submit" class="btn btn-primary">
        {{ hasCandidated ? fa.icon('pen-to-square') : fa.icon('person-sign') }}
        {{ hasCandidated ? 'Modifier' : 'Postuler' }}
      </button>
    </div>
    {{ form_end(form) }}
  </div>
{% endblock %}
